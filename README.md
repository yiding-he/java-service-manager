# java-service-manager
Experimental project to manage local java service processes in command line.

Usage:

```bash
java -jar /path/to/java-service-manager.jar --spring.config.location=/path/to/ --spring.profiles.active=prod
```
